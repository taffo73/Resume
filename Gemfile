source "https://rubygems.org"

# Use the GitHub Pages gem to match the Pages build environment
gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-sitemap"
  gem "jekyll-paginate"
  gem "jekyll-gist"
  gem "jekyll-json-feed", "~> 1.0"
  # add any other plugins you actually use here
end
